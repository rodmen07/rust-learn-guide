# Solutions: Capstone Project

The best capstone is a small app you can finish.

One good path is the existing `todo_cli` example in `examples/`:

- define a `Task` struct
- store tasks in a vector
- parse command-line input
- save data to a file
- add tests for the core logic

If you want to build your own project, start with a tiny version and add one feature at a time.